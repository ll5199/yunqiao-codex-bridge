# 云桥（熙楠）v1.4.7

- 修复智能路由选中的 OpenAI 上游因五小时额度耗尽或临时冷却而返回 HTTP 503 时，Codex 直接中断对话的问题。
- 仅对“智能路由”自动降级到 `grok-4.6`；用户手动选择的模型保持原样，不会被静默切换。
- 当主模型和备用模型都不可用时，将 Sub2API 的通用 503 转换为可读的中文诊断信息，提示检查分组账号、额度、冷却状态和模型映射。
- 保留 Grok 原生 Responses 与 Codex 工具调用能力，文件检索、工作区操作和其他工具不会被降级为纯文字说明。
