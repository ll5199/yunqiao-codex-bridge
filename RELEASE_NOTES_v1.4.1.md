# 云桥（熙楠）v1.4.1

- 新增云桥账号登录入口，可读取有效订阅、分组、上游 Key 与每日/每周/每月用量。
- 登录后可在 ChatGPT、Gemini、Grok 上游间切换，并读取对应分组中的模型后启动 Codex。
- 保留外部 API 模式，可继续手动填写 API 地址和 Key。
- 重构新版 Codex 模型注入：仅处理明确的模型目录、`model/list` 响应和模型白名单配置，不再递归修改页面对象或扫描 React 内部状态。
- 兼容新版 Codex 的 `use-host-config`、`app-server-manager-signals` 与备用应用模块；找不到私有模块时自动安全降级。
- 增加 CDP 桥接健康检查与自动重注入，Codex 页面刷新或 WebView 重建后可自动恢复模型桥接。
- 自定义模型描述会继承 Codex 已有模型字段，降低模型菜单出现空白项或无效项的概率。
- 优化账号模式、外部 API 模式、广告位、模型选择和用量信息的窗口布局。
