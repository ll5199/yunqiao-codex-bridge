# 云桥（熙楠）v1.5.8

- 修复模糊任务权重抽取仍使用固定旧模型列表的问题。
- 从 API 实时返回且在策略中启用的模型里抽取；删除或禁用模型后自动按剩余权重计算。
- 新加入的模型可参与智能路由判断，只有在 `ambiguous_distribution.weights` 中设置正权重后才参与模糊任务抽取。
- 模糊任务按远程策略的 Luna 90、Sol 10 配置抽取；明确任务继续按任务内容分配。
