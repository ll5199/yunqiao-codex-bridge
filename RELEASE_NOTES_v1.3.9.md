# 云桥（熙楠）v1.3.9

- 广告内容改为从 `https://index.velyn65.com/download/ad.json` 异步读取。
- 支持远程修改广告开关、标题、说明、按钮文字和跳转地址，无需重新发布客户端。
- 远程读取使用 3 秒超时，不阻塞客户端启动。
- 远程配置成功后保存在本机；网络异常时继续使用上次缓存，首次运行则使用内置默认广告。
- 广告跳转地址只接受 HTTPS，远程 JSON 格式错误时不会覆盖有效缓存。
- `image_url` 和 `background_color` 已作为兼容字段保留；当前横幅仍使用客户端统一样式。
