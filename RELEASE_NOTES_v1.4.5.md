# 云桥（熙楠）v1.4.5

- 检查更新和下载更新失败时都会自动重试三次，降低临时网络抖动导致的更新失败。
- 下载总超时由 90 秒延长到 5 分钟，兼容速度较慢或跨地区的网络线路。
- 更新先写入 `.part` 临时文件，只有文件完整下载并落盘后才替换待安装文件，避免残缺文件进入安装流程。
- 新增响应长度完整性检查和空文件检查，失败提示会明确显示已经连续重试三次。
- 限制更新下载重定向仍只能停留在云桥允许的 HTTPS 域名，避免重试机制降低下载安全性。
