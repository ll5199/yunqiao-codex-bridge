# 云桥（熙楠）v1.5.2

- 修复移除 Grok 模型后“智能路由（Auto）”从 Codex 模型列表消失的问题。
- Auto 现在只要存在至少一个可用路由模型就会显示，并会自动跳过已删除或禁用的模型。
- 新增 `routing-policy.json` 的 `api_base_url`，更换上游 API 地址无需修改客户端。
- 新增 `model_settings`，可远程设置模型启用状态、用途类型和默认推理强度。
- 保留远程路由规则、Luna 分类器、低置信度兜底和会话模型粘性。
