# 云桥（熙楠）v1.4.6

## 汉化可靠性修复

- 修复 Codex 启动成功但界面仍然全英文的问题。
- 首次写入官方中文设置后自动完整重启一次 Codex，解决只刷新渲染页面导致的部分汉化。
- 官方内置 `zh-CN` 语言包仍优先使用，并将语言设置重试时间延长到 120 秒。
- 增加应用界面控件的中文兜底翻译；不会修改对话正文、提示词、代码、编辑器、终端或模型输出。
- “原生账号启动”现在也会加载中文界面，但仍然不经过云桥 API 代理。
- 启动器会读取页面汉化状态后再报告成功，不再把“脚本执行未报错”当成“汉化已生效”。
- 修复 Electron 菜单接口暂不可用时被错误判断为汉化成功的问题，并增加多种安全加载方式与持续重试。

## 验证

- JavaScript 语法检查通过。
- Go 单元测试通过。
- Windows x64 构建通过。
